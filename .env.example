  # Postgres — these names match the official postgres Docker image,
  # so one file configures both the container and the Python client.
POSTGRES_USER=nba
POSTGRES_PASSWORD=password
POSTGRES_DB=nba
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

NBA_API_DELAY=0.7